def calculate_sum():
    total_sum = 0  # Renamed the variable to avoid conflict
    
    numbers = [1, 2, 3, 4]
    total = sum(numbers)  # Used the built-in sum function
    print("The sum of the numbers is:", total)
    
    more_numbers = [10, 20, 30]
    result = sum(more_numbers)  # Used the built-in sum function
    print("The result is:", result)

calculate_sum()